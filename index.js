const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

const random = Math.ceil(Math.random() * 100);

const num3 = 64;
const num4 = 60;
const mod = num3 % num4;

const max = Math.max(1, 2, 3, 4, 10, 20);

module.exports = {
  multiply,
  random,
  mod,
  max
};

